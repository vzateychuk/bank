plugins {
    id 'java'
}

group = 'vez'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

apply from: "$rootDir/common-dependencies.gradle"

dependencies {
    implementation project(':common')
}

test {
    useJUnitPlatform()
}